package net.txsla.proxychat;

public class commands {
    /*
    *  To add:
    *       /rank ... - ProxyChat ranks commands (only when enabled)
    *       /msg <player> <message> - can send direct messages across the proxy
    *       /mute <?duration> - mute player; OP only, can be disabled in configs
    *       /broadcast <channel, proxy> <message> - broadcasts message to specified channel, or entire proxy
    *       /count reset <player> - get a player's spamLimiter counter
    *       /count add <player> - modify a players spamLimiter counter
    *       /count set <player> - set a player's spamLimiter counter
    *       /count get <player> - returns a player's spamLimiter counter
    *  */
}
